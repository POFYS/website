networks:
  pofys-network:
    driver: bridge

services:
  nginx-proxy:
    image: nginx:alpine
    container_name: pofys-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./.keys:/etc/nginx/.keys:ro
    networks:
      - pofys-network
    depends_on:
      - pofys
      - strapi
      - webhook
    restart: unless-stopped

  pofys:
    build:
      context: ./frontend
      dockerfile: Dockerfile.frontend
    container_name: pofys
    env_file:
      - ./frontend/.env
    volumes:
      - ./frontend:/app
      - ./frontend/nginx/frontend.nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - strapi
    networks:
      - pofys-network
    restart: unless-stopped
    working_dir: /app
    command: >
      sh -c "mkdir -p /app/dist &&
             rm -f /etc/nginx/conf.d/default.conf &&
             nginx -g 'daemon off;'"

  strapi:
    image: node:22-alpine
    container_name: pofys-strapi
    restart: unless-stopped
    working_dir: /srv/app
    command: sh -c "npm install && npm run build && npm run start"
    # command: sh -c "npm install && npm run develop"
    env_file:
      - ./cms/.env
    volumes:
      - ./cms:/srv/app
      - strapi-uploads:/srv/app/public/uploads
    ports:
      - '1337:1337'
    depends_on:
      - postgres
    networks:
      - pofys-network

  postgres:
    image: postgres:18-alpine
    container_name: pofys-postgres
    restart: unless-stopped
    env_file:
      - ./cms/.env
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - pofys-network

  webhook:
    build:
      context: ./frontend
      dockerfile: Dockerfile.webhook
    container_name: pofys-webhook
    restart: unless-stopped
    env_file:
      - ./frontend/.env
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - WEBHOOK_PORT=9000
      - REBUILD_COMMAND=docker exec pofys /app/rebuild.sh
    networks:
      - pofys-network
    depends_on:
      - pofys

  tor:
    build:
      context: ./tor
      dockerfile: Dockerfile.tor
    container_name: pofys-tor
    volumes:
      - tor-data:/var/lib/tor/hidden_service/
    networks:
      - pofys-network
    restart: unless-stopped

volumes:
  postgres-data:
  strapi-uploads:
  tor-data:
